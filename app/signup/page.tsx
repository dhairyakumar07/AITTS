"use client";
import Link from "next/link";import {useState} from "react";import {useRouter} from "next/navigation";import {BarChart3,CheckCircle2,ShieldCheck,Sparkles} from "lucide-react";
export default function Signup(){const r=useRouter();const [name,setName]=useState('');const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [error,setError]=useState('');const [loading,setLoading]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setLoading(true);setError('');const res=await fetch('/api/auth/signup',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({name,email,password})});const d=await res.json();if(!res.ok){setError(d.error);setLoading(false);return}r.push('/dashboard')}return <main className="auth-wrap"><div className="auth-grid"><aside className="auth-side"><div className="brand"><span className="brand-mark">A</span><span><b style={{fontSize:18}}>AITTS</b><small style={{color:'#bdb7d2'}}>All India Test Series</small></span></div><h2>Build a better testing habit.</h2><p>Create your account once. Your tests, attempts and performance history stay together.</p><div className="auth-bullets"><div><CheckCircle2 size={17}/> Instant test access</div><div><BarChart3 size={17}/> Track every attempt</div><div><ShieldCheck size={17}/> Secure independent account</div><div><Sparkles size={17}/> Built for focused preparation</div></div></aside><section className="auth-card"><div className="auth-brand"><span className="brand-mark">A</span> Join AITTS</div><h1>Create account</h1><p className="muted">Set up your student account in under a minute.</p>{error&&<div className="error" style={{marginTop:18}}>{error}</div>}<form className="form" onSubmit={submit} style={{marginTop:24}}><div><label className="label">Full name</label><input className="input" autoComplete="name" value={name} onChange={e=>setName(e.target.value)} required/></div><div><label className="label">Email address</label><input className="input" type="email" autoComplete="email" value={email} onChange={e=>setEmail(e.target.value)} required/></div><div><label className="label">Password</label><input className="input" type="password" autoComplete="new-password" minLength={8} value={password} onChange={e=>setPassword(e.target.value)} required/></div><button className="btn btn-primary" disabled={loading}>{loading?'Creating…':'Create account'}</button></form><p className="footer-note">Already have an account? <Link href="/login" className="gradient-text">Sign in</Link></p></section></div></main>}
