"use client";
import {useEffect,useState} from "react";
export default function Profile(){const [user,setUser]=useState<any>();useEffect(()=>{fetch('/api/auth/me').then(r=>r.json()).then(d=>{if(!d.user)location.href='/login';else setUser(d.user)})},[]);return <main className="container main"><div className="card" style={{padding:28,maxWidth:720}}><p className="muted">Personal details</p><h1>Your profile</h1><div className="form"><div><label className="label">Name</label><input className="input" value={user?.name||''} readOnly/></div><div><label className="label">Email</label><input className="input" value={user?.email||''} readOnly/></div><div><label className="label">Account role</label><input className="input" value={user?.role||''} readOnly/></div></div><p className="footer-note">Profile editing can be enabled through the admin policy layer later.</p></div></main>}
