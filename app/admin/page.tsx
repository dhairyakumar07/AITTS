"use client";
import Link from "next/link";import {useEffect,useState} from "react";import {Plus,Users,BarChart3,FileText} from "lucide-react";
export default function Admin(){const [tests,setTests]=useState<any[]>([]);useEffect(()=>{fetch('/api/admin/tests').then(r=>r.json()).then(d=>{if(d.error)location.href='/login';else setTests(d.tests||[])})},[]);const attempts=tests.reduce((a,t)=>a+(t.attempt_count||0),0);return <main className="container main"><div className="dashboard-head"><div><p className="muted">Host / Admin</p><h1>AITTS control center</h1></div><Link className="btn btn-primary" href="/admin/tests/new"><Plus size={17}/>Create test</Link></div><div className="grid grid-4" style={{marginTop:22}}><div className="card stat"><FileText size={18}/><div className="num">{tests.length}</div><div className="muted">Tests</div></div><div className="card stat"><Users size={18}/><div className="num">{attempts}</div><div className="muted">Attempts</div></div><div className="card stat"><BarChart3 size={18}/><div className="num">{tests.filter(t=>t.status==='PUBLISHED').length}</div><div className="muted">Published</div></div><div className="card stat"><div className="num">{tests.filter(t=>t.status==='CLOSED').length}</div><div className="muted">Closed</div></div></div><div className="section-title"><h2>Manage tests</h2></div><div className="card table-wrap"><table className="table"><thead><tr><th>Test</th><th>Status</th><th>Questions</th><th>Attempts</th><th>Action</th></tr></thead><tbody>{tests.map(t=><tr key={t.id}><td><b>{t.title}</b><div className="muted">{t.subject} · {t.access_code}</div></td><td><span className="pill">{t.status}</span></td><td>{t.question_count}</td><td>{t.attempt_count}</td><td><Link className="btn btn-secondary" href={`/admin/tests/${t.id}`}>Manage</Link></td></tr>)}</tbody></table></div></main>}
