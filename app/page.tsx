import Link from "next/link";
export default function Home(){return <main className="page"><section className="container main"><div className="card hero"><span className="pill">ALL INDIA TEST SERIES</span><h1>Test smarter. <span className="gradient-text">Perform stronger.</span></h1><p>AITTS is a standalone online examination platform for structured tests, secure server-side grading, detailed results, and a clean student experience.</p><div style={{display:'flex',gap:10,marginTop:22,flexWrap:'wrap'}}><Link className="btn btn-primary" href="/login">Login</Link><Link className="btn btn-secondary" href="/signup">Create account</Link></div></div><div className="grid grid-3" style={{marginTop:18}}>{[['Secure','Server-side grading keeps answer keys away from students.'],['Interactive','Timed tests, question navigation and review states.'],['Insightful','Results, attempts and host analytics in one place.']].map(x=><div className="card stat" key={x[0]}><span className="pill">{x[0]}</span><p className="muted">{x[1]}</p></div>)}</div></section></main>}
