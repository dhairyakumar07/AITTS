"use client";
import Link from "next/link";import {useState} from "react";import {useRouter} from "next/navigation";import {BarChart3,CheckCircle2,ShieldCheck,Sparkles} from "lucide-react";
export default function Login(){const r=useRouter();const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [error,setError]=useState('');const [loading,setLoading]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setLoading(true);setError('');const res=await fetch('/api/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email,password})});const d=await res.json();if(!res.ok){setError(d.error);setLoading(false);return}r.push(d.user.role === 'ADMIN' ? '/admin' : '/dashboard')}return <main className="auth-wrap"><div className="auth-grid"><aside className="auth-side"><div className="brand"><span className="brand-mark">A</span><span><b style={{fontSize:18}}>AITTS</b><small style={{color:'#bdb7d2'}}>All India Test Series</small></span></div><h2>Walk into every test prepared.</h2><p>One place for serious attempts, clean analytics and exam-focused practice.</p><div className="auth-bullets"><div><CheckCircle2 size={17}/> Timed JEE-style tests</div><div><BarChart3 size={17}/> Performance tracking</div><div><ShieldCheck size={17}/> Secure server-side grading</div><div><Sparkles size={17}/> A distraction-free interface</div></div></aside><section className="auth-card"><div className="auth-brand"><span className="brand-mark">A</span> Welcome to AITTS</div><h1>Sign in</h1><p className="muted">Continue to your preparation dashboard.</p>{error&&<div className="error" style={{marginTop:18}}>{error}</div>}<form className="form" onSubmit={submit} style={{marginTop:24}}><div><label className="label">Email address</label><input className="input" type="email" autoComplete="email" value={email} onChange={e=>setEmail(e.target.value)} required/></div><div><label className="label">Password</label><input className="input" type="password" autoComplete="current-password" value={password} onChange={e=>setPassword(e.target.value)} required/></div><button className="btn btn-primary" style={{marginTop:4}} disabled={loading}>{loading?'Signing in…':'Sign in'}</button></form><p className="footer-note"><Link href="/forgot-password" className="gradient-text">Forgot password?</Link></p><p className="footer-note">New to AITTS? <Link href="/signup" className="gradient-text">Create your account</Link></p></section></div></main>}
