"use client";
import Link from "next/link";import {Moon,Sun,LogOut,ShieldCheck} from "lucide-react";import {useEffect,useState} from "react";import {useTheme} from "./ThemeProvider";
export function AppHeader(){const {theme,toggle}=useTheme();const [user,setUser]=useState<any>(null);useEffect(()=>{fetch('/api/auth/me').then(r=>r.json()).then(x=>setUser(x.user))},[]);async function logout(){await fetch('/api/auth/logout',{method:'POST'});location.href='/'}return <header className="topbar"><div className="container topbar-inner"><Link href="/" className="brand"><span className="brand-dot"/>AITTS</Link><div className="nav-actions">{user&&<><Link className="btn btn-secondary hide-mobile" href={user.role==='ADMIN'?'/admin':'/dashboard'}>{user.role==='ADMIN'?<><ShieldCheck size={16}/>Host</>:"Dashboard"}</Link><button className="theme-btn" onClick={toggle} title="Toggle theme">{theme==='dark'?<Sun size={18}/>:<Moon size={18}/>}</button><button className="btn btn-secondary" onClick={logout}><LogOut size={16}/><span className="hide-mobile">Logout</span></button></>}</div></div></header>}
